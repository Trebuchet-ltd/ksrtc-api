{
  "routes": [
    {
      "method": "GET",
      "path": "/conductors",
      "handler": "conductor.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/conductors/count",
      "handler": "conductor.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/conductors/:id",
      "handler": "conductor.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/conductors",
      "handler": "conductor.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/conductors/:id",
      "handler": "conductor.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/conductors/:id",
      "handler": "conductor.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
